import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Otrebor050607.albumtracker26',
  appName: 'Album Tracker 26',
  webDir: 'dist'
};

export default config;
